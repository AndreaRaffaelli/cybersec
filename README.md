# PortScan Detection with EBPF

## How To

- Be sure to have vagrant installed: [Debian's tutorial](https://wiki.debian.org/Vagrant)
- Install NFS-server on your host machine ``` sudo apt-get install nfs-kernel-server ```
